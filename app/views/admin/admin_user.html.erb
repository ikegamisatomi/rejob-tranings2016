<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <Div Align="right">
    <a href="admin">adminトップへ</a>
    </Div>

    <h1>求職者管理一覧</h1>

  <br>
  <h2>登録者数　<%=  User.count %>名</h2>

    <table border=1 height="200" width="500">
    <tr style="background:#BBBBBB">
     <td width="50">求職者ID</td>
     <td>氏名</td>
     <td>年齢</td>
     <td>性別</td>
     <td style="width:100px">edit</td>
    </tr>

      <% User.all.each do |user| %>
     <tr>
       <td align="center"><%= user.id %></td>
       <td><%= user.name %></td>
       <td align="center"><%= user.age %></td>
       <td align="center"><%= user.sex %></td>
       <td align="center"><a href="/edit_user?id=<%= user.id %>">edit</td>
    <% end %>

  </body>
</html>
