<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>求職者管理一覧</h1>

  <br>
  登録者数　<%=  User.count %>名

    <table border=1 >
    <tr>
     <td>求職者ID</td>
     <td>氏名</td>
     <td>年齢</td>
     <td>性別</td>
     <td style="width:100px">edit</td>
    </tr>

      <% User.all.each do |user| %>
     <tr>
       <td><%= user.id %></td>
       <td><%= user.name %></td>
       <td><%= user.age %></td>
       <td><%= user.sex %></td>
       <td><a href="/edit_user?id=<%= user.id %>">edit</td>
    <% end %>

  </body>
</html>
